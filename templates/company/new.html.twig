{% extends 'base.html.twig' %}

{% block body %}

{{ include('company/form.html.twig') }}

  {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
  {{ form_widget(form) }}
  {{ form_end(form) }}
{% endblock %}
